import { Check } from "lucide-react"; import { Container } from "@/components/layout/Container"; import { modernizationServices } from "@/config/modernization"; import { ModernizationIcon } from "./modernization-icons"; import { ModernizationSectionHeader } from "./ModernizationSectionHeader";
export function ModernizationServices() { return <section className="border-b border-border bg-background py-20 sm:py-28"><Container><ModernizationSectionHeader eyebrow="End-to-end capability" title="Our Legacy Modernization Capabilities" description="Assess, redesign, migrate, and optimize applications through one governed transformation program." /><div className="mt-12 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{modernizationServices.map((service,index) => <article key={service.title} className={`rounded-2xl border bg-card p-6 shadow-card ${index===0?"border-primary/45 shadow-glow-primary":"border-border"}`}><div className="flex items-center gap-4"><span className="flex size-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><ModernizationIcon name={service.icon} className="size-5" /></span><div><p className="font-mono text-[9px] font-bold tracking-widest text-primary">CAPABILITY {String(index+1).padStart(2,"0")}</p><h3 className="mt-1 text-lg font-bold">{service.title}</h3></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{service.description}</p><ul className="mt-5 grid gap-2 border-t border-border pt-5">{service.features.map(feature => <li key={feature} className="flex items-center gap-2 text-sm"><Check className="size-3.5 text-primary" />{feature}</li>)}</ul></article>)}</div></Container></section>; }
