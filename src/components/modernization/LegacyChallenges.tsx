import { Container } from "@/components/layout/Container"; import { legacyChallenges } from "@/config/modernization"; import { ModernizationIcon } from "./modernization-icons"; import { ModernizationSectionHeader } from "./ModernizationSectionHeader";
export function LegacyChallenges() { return <section className="border-b border-border bg-surface/30 py-20 sm:py-24"><Container><ModernizationSectionHeader eyebrow="The cost of standing still" title="Why Organizations Modernize Legacy Applications" description="Modernization reduces operational risk while creating the speed, security, and connectivity required for the next stage of growth." /><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-5">{legacyChallenges.map(item => <article key={item.title} className="rounded-2xl border border-border bg-card p-6 shadow-card transition duration-300 hover:-translate-y-1 hover:border-primary/45 hover:shadow-glow-primary"><span className="flex size-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><ModernizationIcon name={item.icon} className="size-5" /></span><h3 className="mt-6 font-bold">{item.title}</h3><p className="mt-3 text-sm leading-7 text-muted-foreground">{item.description}</p></article>)}</div></Container></section>; }
