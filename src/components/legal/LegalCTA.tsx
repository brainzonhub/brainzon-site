import Link from "next/link";
import { ArrowRight, MessagesSquare } from "lucide-react";
export function LegalCTA({ security = false }: { security?: boolean }) { return <aside className="legal-cta mt-10 rounded-2xl border border-primary/25 bg-primary/[.055] p-6 print:hidden"><MessagesSquare className="size-5 text-primary" /><h2 className="mt-4 text-xl font-bold">{security ? "Have a security concern?" : "Questions about these terms?"}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{security ? "Contact our security team for coordinated, responsible disclosure." : "Contact Brainzon for clarification about this document or how it applies to a service."}</p><Link href={security ? "mailto:security@brainzon.com" : "mailto:info@brainzon.com"} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground transition-colors hover:bg-primary/85">Contact Brainzon <ArrowRight className="size-4" /></Link></aside>; }
