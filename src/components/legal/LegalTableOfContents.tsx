import type { LegalSectionData } from "@/config/legal";
export function LegalTableOfContents({ sections, mobile = false }: { sections: readonly LegalSectionData[]; mobile?: boolean }) { const list = <ol className="grid gap-1.5">{sections.map((section,index) => <li key={section.id}><a href={`#${section.id}`} className="group flex items-start gap-3 rounded-lg px-3 py-2 text-sm text-muted-foreground transition-colors hover:bg-primary/10 hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><span className="mt-0.5 font-mono text-[10px] font-bold text-primary">{String(index+1).padStart(2,"0")}</span><span>{section.title}</span></a></li>)}</ol>; if (mobile) return <details className="rounded-xl border border-border bg-card p-4 shadow-card lg:hidden print:hidden"><summary className="cursor-pointer text-sm font-bold">On this page</summary><div className="mt-4 border-t border-border pt-3">{list}</div></details>; return list; }
