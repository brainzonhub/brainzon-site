import { Check } from "lucide-react";
import type { LegalSectionData } from "@/config/legal";
import { CopySectionLink } from "./CopySectionLink";
export function LegalSection({ section, index }: { section: LegalSectionData; index: number }) { return <section id={section.id} className="legal-section scroll-mt-24 border-b border-border py-9 first:pt-0 last:border-0 last:pb-0"><div className="flex items-start justify-between gap-4"><div className="flex items-start gap-3"><span className="mt-1 font-mono text-xs font-bold text-primary">{String(index+1).padStart(2,"0")}</span><h2 className="text-2xl font-bold tracking-tight text-foreground">{section.title}</h2></div><CopySectionLink sectionId={section.id} /></div><div className="mt-5 space-y-4 text-[15px] leading-7 text-muted-foreground">{section.paragraphs.map(paragraph => <p key={paragraph}>{paragraph}</p>)}</div>{section.items && <ul className="mt-5 grid gap-2 sm:grid-cols-2">{section.items.map(item => <li key={item} className="flex items-start gap-2 rounded-lg border border-border bg-surface/70 px-3 py-2.5 text-sm leading-6"><Check className="mt-1 size-3.5 shrink-0 text-primary" /><span>{item}</span></li>)}</ul>}{section.groups && <div className="mt-6 grid gap-4 md:grid-cols-2">{section.groups.map(group => <div key={group.title} className="rounded-xl border border-border bg-surface/70 p-5"><h3 className="font-bold text-foreground">{group.title}</h3><ul className="mt-3 grid gap-2">{group.items.map(item => <li key={item} className="flex items-start gap-2 text-sm leading-6 text-muted-foreground"><Check className="mt-1 size-3.5 shrink-0 text-primary" />{item}</li>)}</ul></div>)}</div>}</section>; }
